## Debug Session: 20260402_192733_681805

### Root Cause
Questions are stored as Quill rich-text HTML (including KaTeX math via `ql-formula`) on the backend.
All question render sites used `<pre>{question}</pre>` which renders raw HTML as plain text.

---

## Changes Made

### 1. Fixed question rendering — `Question.js`
- **File:** `src/pages/question/Question.js`
- **Change 1 (line ~698):** Replaced `<pre>{thisQuestion?.question}</pre>` with `<div className="ql-editor" dangerouslySetInnerHTML={{ __html: thisQuestion?.question }} />`
- **Change 2 (line ~844):** Replaced `<pre>{item.question}</pre>` (pocket list) with same pattern.
- **Change 3 (getQuestionLines):** Added `stripHtml()` helper that uses a temp DOM element to extract plain text from HTML, used in flash mode before splitting by `\n`.
- **Revert:** Restore the three `<pre>` usages and remove `stripHtml`.

### 2. Fixed question rendering — `Assignment.js`
- **File:** `src/pages/assignment/Assignment.js`
- **Change 1 (line ~1036):** Replaced `<pre>{thisQuestion?.question}</pre>` with `<div className="ql-editor" dangerouslySetInnerHTML={{ __html: thisQuestion?.question }} />`
- **Change 2 (getQuestionLines):** Same `stripHtml()` helper added for flash mode.
- **Revert:** Restore `<pre>` and remove `stripHtml`.

### 3. Fixed question rendering — `TeacherDashboard.js`
- **File:** `src/pages/teacherDashboard/TeacherDashboard.js`
- **Change (line ~370):** Replaced `<pre>{item?.question}</pre>` with `<div className="ql-editor" dangerouslySetInnerHTML={{ __html: item?.question }} />`
- **Revert:** Restore `<pre>` tag.

### 4. Added Quill + KaTeX CSS — `index.html`
- **File:** `public/index.html`
- **Change:** Added CDN `<link>` for `quill@2/dist/quill.snow.css` and `katex@0.16.11/dist/katex.min.css` in `<head>`.
- **Revert:** Remove the two `<link>` tags.

### 5. Added read-only ql-editor CSS reset — `reusable.css`
- **File:** `src/reusable.css`
- **Change:** Added `.question-form-body .ql-editor` and `.form-body-list .ql-editor` rules to strip default Quill editor padding/border when displaying read-only question content.
- **Revert:** Remove the added CSS block.

## Revert Status
- [ ] Change 1 — Question.js rendering + stripHtml
- [ ] Change 2 — Assignment.js rendering + stripHtml
- [ ] Change 3 — TeacherDashboard.js rendering
- [ ] Change 4 — index.html CDN links
- [ ] Change 5 — reusable.css ql-editor reset
